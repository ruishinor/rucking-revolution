---
// Container Props
// children: HTMLElement (default slot)
// class: string (optional)
const { children, class: className = '' } = Astro.props;
---
<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 {{ className }}">
  <slot />
</div>