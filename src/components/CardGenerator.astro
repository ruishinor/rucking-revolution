---
import { crewTactics } from '@/data/crewTactics';
let { category = 'all' } = Astro.props;
---

<div class="space-y-6" id="card-generator-root" data-crew-tactics={JSON.stringify(crewTactics)}>
  <!-- Controls -->
  <div class="flex flex-wrap items-center gap-4">
    <button 
      id="drawOneBtn"
      class="btn btn-primary btn-md"
      aria-label="Draw one card"
    >
      Draw 1 Card
    </button>
    <button 
      id="drawThreeBtn"
      class="btn btn-secondary btn-md"
      aria-label="Draw three cards"
    >
      Draw 3 Cards
    </button>
    <select 
      id="categoryFilter"
      class="rounded-border border border-muted-200 bg-white px-3 py-2 text-foreground focus:outline-none focus:ring-2 focus:ring-accent"
      aria-label="Filter by category"
    >
      <option value="all" selected={category === 'all'}>All Categories</option>
      <option value="Leadership" selected={category === 'Leadership'}>Leadership</option>
      <option value="Communication" selected={category === 'Communication'}>Communication</option>
      <option value="Decision Making" selected={category === 'Decision Making'}>Decision Making</option>
      <option value="Adaptability" selected={category === 'Adaptability'}>Adaptability</option>
    </select>
    <button 
      id="shuffleBtn"
      class="btn btn-utility btn-md"
      aria-label="Shuffle deck"
    >
      Shuffle Deck
    </button>
  </div>

  <!-- Cards Container -->
  <div id="cardsContainer" class="grid gap-6 swipe-area" data-swipe-enabled="true" role="region" aria-label="Drawn tactic cards">
    <!-- Cards will be populated by client-side JavaScript -->
  </div>

  <!-- Status -->
  <div id="status" class="text-center text-muted" aria-live="polite">
    <span class="sr-only">Loading cards</span>
  </div>
</div>

<!-- Client-side script for interactivity -->
<script type="module" src="/scripts/card-generator.js" is:inline></script>
